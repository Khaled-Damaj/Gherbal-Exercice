import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  chart_container: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
    margin: "30px 0px",
  },
}));
